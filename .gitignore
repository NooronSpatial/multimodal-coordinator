.DS_Store
/.build
/Packages
xcuserdata/
DerivedData/
.swiftpm/
.netrc

# A local clone of this repo that dev tooling drops here. 500 MB, not
# ours to version, and it was committed once as a stray gitlink (fixed
# forward in the commit below). Ignored so it cannot happen twice.
local_clone/

# The MLX shader library (D-062 F-3 = A): BUILT, never vendored. See
# Scripts/metallib.sh. A 3.6 MB binary has no business in a repo whose
# method is reproducibility.
default.metallib
